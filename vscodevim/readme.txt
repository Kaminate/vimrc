
settings.json // "C:\Users\Nate\AppData\Roaming\Code\User\settings.json"
  // https://github.com/VSCodeVim/Vim
  // When you're over a fold and press down ( hjkl ),
  // it opens the fold instead of skipping over it.
  // This hack fixes it ( but there are side fx )
  vim.foldfix: true
 
keybindings.json

  if you cant find it, just edit a keybinding.
    1. (ctrl shift p) open preferences
    2. Preferences: Open Keyboard Shortcuts
    3. Change something lol
  that creates the .json that you can now replace.


